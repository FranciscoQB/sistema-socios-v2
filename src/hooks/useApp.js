// src/hooks/useApp.js
import { useContext } from 'react';
import { AppContext } from '../context/AppContext';

/**
 * Hook para acceder al contexto de la aplicación
 * Ya está definido en AppContext, pero lo exportamos aquí por conveniencia
 */
export { useApp } from '../context/AppContext';